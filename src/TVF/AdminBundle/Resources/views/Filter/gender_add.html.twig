<form class="form-signin" name="form" method="post" action="{{ path('tvf_admin_gender_add',{'id': id}) }}">
  {% set input_form="TVFAdminBundle:Utils:form_input.html.twig" %}
  {% set save_form="TVFAdminBundle:Utils:form_save.html.twig" %}
  <h6 class="form-signin-heading">{% if id == 0 %}Ajouter{% else %}Modifier{% endif %} un genre</h6>
  {{ include(input_form, {'element': form.name, 'name':"Jazz, Pop, Rap..."}) }}
  {{ include(save_form, {'element': form.save, 'name':"Ajouter"}) }}
  {{ form_rest(form) }}
</form>
